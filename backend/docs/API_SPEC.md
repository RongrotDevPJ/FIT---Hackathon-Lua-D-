# 📡 API Specification – LonganLink Backend

Backend สร้างบน **Firebase Functions (TypeScript)**  
ฐานข้อมูลหลัก: **Firestore**

---

## 1️⃣ User API

### ➕ POST `/userApi`
**Purpose:** สร้างผู้ใช้ใหม่ (เกษตรกร / โรงงาน)

**Request:**
```json
{
  "name": "สมชาย ฟาร์มลำไย",
  "phone": "0812345678",
  "role": "farmer",
  "province": "เชียงใหม่",
  "amphoe": "แม่อาย"
}
2️⃣ Order API
➕ POST /orderApi/orders
Purpose: เพิ่มออเดอร์ขายหรือรับซื้อ
Request:
{
  "type": "sell",
  "ownerId": "user12345",
  "province": "เชียงใหม่",
  "amphoe": "แม่อาย",
  "grade": "B",
  "amountKg": 500,
  "requestedPrice": 12.5
}

Response:
{
  "id": "order001",
  "status": "open"
}


🔍 GET /orderApi/orders/my?ownerId=user12345
Purpose: ดึงออเดอร์ของผู้ใช้คนใดคนหนึ่ง
Response:
[
  {
    "id": "order001",
    "type": "sell",
    "grade": "B",
    "amountKg": 500,
    "status": "open"
  }
]


🔄 GET /orderApi/orders/match?ownerId=factory001&role=factory
Purpose: ดึงรายการขาย (sell) ที่อยู่ในจังหวัด/อำเภอเดียวกัน
Response:
[
  {
    "id": "order002",
    "farmer": "นายแดง ฟาร์มสวนลำไย",
    "province": "เชียงใหม่",
    "amphoe": "แม่อาย",
    "requestedPrice": 11.0
  }
]


3️⃣ Negotiation API
➕ POST /negotiationApi/negotiations
Purpose: โรงงานส่งคำขอต่อรองราคา
Request:
{
  "orderId": "order002",
  "fromId": "factory001",
  "toId": "farmer001",
  "requestedPrice": 10.8
}

Response:
{
  "id": "nego009",
  "status": "pending"
}


✳️ POST /negotiationApi/negotiations/respond
Purpose: เกษตรกรตอบกลับคำขอต่อรองราคา
Request:
{
  "negotiationId": "nego009",
  "action": "accept",
  "newPrice": 10.8
}

Response:
{
  "status": "accepted",
  "finalPrice": 10.8
}


4️⃣ Health Check
✅ GET /healthCheck
Purpose: ตรวจว่า backend ยังทำงานอยู่
Response:
{ "status": "ok", "service": "LonganLink backend" }


🔐 Auth & Security (เบื้องต้น)


ทุก API ต้องมี ownerId ที่ถูกต้อง


Firestore Rules:


ผู้ใช้แก้ไขได้เฉพาะข้อมูลตัวเอง


ดูข้อมูลออเดอร์ที่เป็นสาธารณะเท่านั้น





---

## 📄 **3. MATCHING_LOGIC.md**

> อธิบายการทำงานของระบบจับคู่และการต่อรองราคา

```markdown
# 🔄 Matching & Negotiation Logic

## 🎯 เป้าหมาย
ช่วย “จับคู่” เกษตรกรที่มีลำไยขาย กับ “โรงงาน” ที่ต้องการรับซื้อ  
โดยใช้เงื่อนไข **จังหวัด–อำเภอ** เป็นตัวกรองหลัก  
เพื่อให้การขนส่งสะดวกและลดของเสีย (Food Loss)

---

## 🧩 ขั้นตอนการจับคู่ (Matching Logic)

1. โรงงานเข้าสู่ระบบ (role = factory)
2. Backend จะดึงข้อมูลจาก Firestore:
   - อ่าน province และ amphoe ของโรงงาน
3. ค้นหา orders ที่ type = `"sell"` และ
   - `province` == โรงงาน
   - `amphoe` == โรงงาน (priority สูงสุด)
4. ถ้าไม่มี order ในอำเภอเดียวกัน → ขยายค้นหาเฉพาะจังหวัดเดียวกัน
5. ส่งกลับ list ของ orders เรียงตาม:
   - ราคาที่เกษตรกรตั้ง (`requestedPrice`)
   - ปริมาณ (`amountKg`)  

---

## 💬 การต่อรองราคา (Negotiation Flow)

1. โรงงานเลือก order ที่สนใจ → ส่งคำขอต่อรองผ่าน API  
   → `status = "pending"`
2. เกษตรกรเห็นคำขอในหน้า “แจ้งเตือนการต่อรอง”
   - ถ้าตกลง → `status = "accepted"`, `finalPrice` ถูกบันทึก
   - ถ้าปฏิเสธ → `status = "rejected"`
   - ถ้าต่อรองกลับ → `status = "counter"`, ตั้งราคาใหม่
3. เมื่อมีการ `accepted` แล้ว → อัปเดต order:
   - `status = "matched"`
   - `matchedAt = timestamp`

---

## 📊 ตัวอย่างผลลัพธ์ใน Firestore

### orders
```json
{
  "id": "order001",
  "type": "sell",
  "province": "เชียงใหม่",
  "amphoe": "แม่อาย",
  "requestedPrice": 11.5,
  "status": "matched"
}

negotiations
{
  "id": "nego001",
  "orderId": "order001",
  "fromId": "factory01",
  "toId": "farmer01",
  "requestedPrice": 11.0,
  "finalPrice": 11.2,
  "status": "accepted"
}


⚙️ สรุปภาพรวมระบบ (Flow)
[Farmer] --โพสต์ขาย--> [Order.sell] 
         ⬇️
     [Matching Logic]
         ⬇️
[Factory] --เสนอราคา--> [Negotiation]
         ⬇️
[Farmer] --ตอบกลับ (Accept/Reject/Counter)-->
         ⬇️
  [FinalPrice Confirmed] → Order.status = matched


💡 จุดเด่นระบบ


ลดระยะทางขนส่งด้วย matching ตามจังหวัด/อำเภอ


สร้างระบบต่อรองราคาที่โปร่งใส (ราคาสุดท้ายบันทึกไว้ในระบบ)


ลด Food Loss เพราะของไม่ขายทิ้งเปล่า แต่ได้ไปแปรรูปในโรงงานที่ใกล้ที่สุด



---

อยากให้ฉันจัด version ที่ “พร้อม push เข้า GitHub จริง” (มีไฟล์ Markdown พร้อมฟอร์แมต) ให้ไหม  
จะจัดโค้ด Markdown + syntax highlight แบบสวยเหมือน production doc เลย 💪
